{
  "name": "scripts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "comment": "PDF generation needs NOTHING here — generate-pdf.mjs runs on bare Node and renders via a browser you already have. 'docx' is only for the optional Word-doc export (generate-cv.mjs); install it with `npm install` only if you want .docx output.",
  "optionalDependencies": {
    "docx": "^9.5.1"
  }
}
